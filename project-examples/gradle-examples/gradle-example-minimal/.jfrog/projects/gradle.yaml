version: 1
type: gradle
resolver:
    repo: zhl-gradle-virtual
    serverId: cka
deployer:
    repo: zhl-gradle-virtual
    serverId: cka
    deployMavenDescriptors: true
    deployIvyDescriptors: true
    ivyPattern: '[organization]/[module]/ivy-[revision].xml'
    artifactPattern: '[organization]/[module]/[revision]/[artifact]-[revision](-[classifier]).[ext]'
